# Houston api-web — Railway prod-test V1
# Wiring: Root Directory /, Config File /infra/railway/api-web/railway.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/railway/Dockerfile.api-web"
watchPatterns = [
  "/apps/web/**",
  "/contracts/**",
  "/apps/api/**",
  "/infra/docker/railway/**",
  "/infra/railway/api-web/**",
  "/pyproject.toml",
  "/uv.lock",
  "/.dockerignore",
  "/README.md",  # temporaire — retiré en PR3 quand COPY README.md disparaît des Dockerfiles
]

[deploy]
startCommand = "/app/infra/docker/railway/start-api-web.sh"
healthcheckPath = "/api/v1/health/"
preDeployCommand = "/bin/sh -c 'cd /app/apps/api && /opt/venv/bin/python manage.py migrate --noinput --verbosity 2'"
