# Houston api-web — Railway prod-test V1
# Wiring: Root Directory /, Config File /infra/railway/api-web/railway.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/railway/Dockerfile.api-web"

[deploy]
# TEMP diagnostic — revert after Railway preDeploy debug
startCommand = "/app/infra/docker/railway/start-api-web-debug.sh"
healthcheckPath = "/api/v1/health/"
# preDeployCommand = "cd /app/apps/api && uv run python manage.py migrate"
