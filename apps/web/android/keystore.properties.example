# Copy to keystore.properties (gitignored). Paths are relative to apps/web/android/.
# Generate an upload keystore once, keep an offline backup, never commit it.
# Play App Signing: this is the upload key. Google holds the app signing key after first Console enrollment.

storeFile=upload-keystore.jks
storePassword=
keyAlias=upload
keyPassword=
