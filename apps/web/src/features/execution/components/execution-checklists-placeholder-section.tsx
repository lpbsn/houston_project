import { ExecutionChecklistPlaceholderCard } from './execution-checklist-placeholder-card'

/** PLACEHOLDER — single entry point to remove or swap when checklists ship. */
export function ExecutionChecklistsPlaceholderSection() {
  return (
    <div className="px-3">
      <ExecutionChecklistPlaceholderCard />
    </div>
  )
}
