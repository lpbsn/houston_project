# Houston audits

Many files in this directory were written **before Lot 10** (legacy Action/Checklist purge). They may reference deleted paths (`apps/api/houston/actions/`, `checklists/`, `features/actions/`, `features/checklists/`, `/execution-feed/`).

**Current stabilization reference (post-Lot 10):** [`lot_11_stabilization_audit.md`](lot_11_stabilization_audit.md)

Treat older audits as historical context unless cross-checked against `apps/api/schema.yml` and live code.
